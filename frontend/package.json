{
  "name": "interview-prep-ai-coach-frontend",
  "version": "1.0.0",
  "description": "Frontend for Interview Prep AI Coach - AngularJS Application",
  "scripts": {
    "start": "http-server -a localhost -p 4200 -c-1",
    "dev": "http-server -a localhost -p 4200 -c-1",
    "build": "echo 'No build step required for AngularJS'",
    "test": "karma start karma.conf.js",
    "test:single": "karma start karma.conf.js --single-run"
  },
  "dependencies": {
    "angular": "^1.8.3",
    "angular-animate": "^1.8.3",
    "angular-route": "^1.8.3",
    "angular-sanitize": "^1.8.3",
    "bootstrap": "^5.3.2",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "karma": "^6.4.2",
    "karma-chrome-launcher": "^3.2.0",
    "karma-jasmine": "^5.1.0",
    "karma-jasmine-html-reporter": "^2.1.0",
    "jasmine-core": "^5.1.1"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}